<h1>All Users (<%= @users.count %>)</h1>
<h3>
  Maybe you want to see the <%= link_to 'newest users', newest_path %>?
</h3>
<h3>
  <%= link_to 'Sign up', new_user_path %>
</h3>
<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.username, user_path(user.username) %></li>
  <% end %>
</ul>
